export const selectedMovieSelector = (state) => state.movieDetail.movieInfo;
export const theaterSelector = (state) => state.movieDetail.theater;
export const scheduleSelector = (state) => state.movieDetail.schedule;
export const movieListColumnSelector = (state) =>
	state.movieDetail.movieListColumn;
